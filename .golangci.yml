version: "2"

linters:
  settings:
    errcheck:
      exclude-functions:
        - fmt.Fprint
        - fmt.Fprintln
        - fmt.Fprintf
        - os.Setenv
        - os.Unsetenv
        - (*encoding/json.Encoder).Encode
