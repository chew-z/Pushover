version: "2"

linters:
  enable:
    - errorlint
  settings:
    errcheck:
      exclude-functions:
        - fmt.Fprint
        - fmt.Fprintln
        - fmt.Fprintf
        - os.Setenv
        - os.Unsetenv
        - (*encoding/json.Encoder).Encode
